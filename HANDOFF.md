# KH Field Notes — 交接文档

这是一个安静、文字优先的个人博客，不是作品集或内容管理系统。

## 内容与发布

- 每篇文章是 `content/` 中的一份 Markdown 文件，文件名即文章 URL。
- 图片放入 `content/media/`，正文使用 `![图片说明](media/文件名.png)` 插入。
- 首页显示最近文章，归档页按时间列出全部文章，文章页只服务阅读。
- 首页显示最近 12 篇；归档每页显示 50 篇，超出时生成静态翻页链接。
- 不再有 Studio、浏览器 localStorage、数据库、登录或发布按钮；编辑 Markdown 后重新构建即可发布。

## 视觉原则

- 白底、深色文字、克制的细线与大量留白。
- 不使用彩色卡片、抽象装饰图、图片拼贴、悬停位移或页面动效。
- 文章比界面更重要；图片仅在文章确有必要时才加入。

## 运行与部署

- `npm run dev`：构建后在 `http://localhost:3000/` 预览。
- `npm run build`：生成 `dist/` 静态文件。
- GitHub Pages 或 Cloudflare Pages 直接发布 `dist/`；没有服务器、数据库或运行时依赖。

## 不要做的事

- 不要重新引入 CMS、登录、数据库或浏览器本地发布状态。
- 不要为了首页显得“有设计感”而压过文章本身。
- 未经用户明确授权，不提交、推送或发布到公网。
